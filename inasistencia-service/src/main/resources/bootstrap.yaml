spring:
  application:
    name: inasistencia-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8091
  datasource:
    url: jdbc:mysql://localhost:33060/inasistenciaServiceBD
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
#server:
#  port: 8092

#Se le indica a Prometheus que agregue todas las métricas a la hora de monitorear
#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
